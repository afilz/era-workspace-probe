# era-workspace-probe

A small repository ERA checks out, so that working in code can be tested against a real
remote rather than a temporary directory.

Nothing here is meant to be useful. What it is meant to be is unambiguous: every file
says something an agent could not have guessed, so an answer containing it is proof the
file was read rather than imagined.
